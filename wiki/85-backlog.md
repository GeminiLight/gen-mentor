<!-- Last verified: 2026-09-19 -->

# Backlog

- [ ] 模型选择器：`/api/health` 只暴露 fast / smart 两个名字。若要在界面里切换模型，需要一个列表接口与档案里的偏好。
- [x] 当前 UI 不再使用模型 `overall_progress`；按已确认目标与当前技能估计派生，课程完成独立显示（2026-09-19）。
- [ ] 模型原始 `overall_progress` 和历史快照仍保留兼容；后续迁移需对照评测，不将本轮 UI 派生修复等同于模型可靠性已解决。
- [ ] 输出格式说明从 zod schema 生成，替代 prompt 里手写的示例 JSON。需要先有对照评测。
- [ ] profiler 的 `behavioral_patterns` 在 init 时是编造的（"每周登录 4 次""对徽章有反应"）。界面已改为只显示档案派生的真实活动（`derive.ts#activitySummary`），但这段文字仍随画像发回模型。prompt 层应让 init 输出"尚无记录"，或由代码把 `activitySummary` 写回画像再发；改 prompt 需重录 fixture。
- [ ] `sessionMinutes` 只有打开与完成两类时间戳，同一次坐下超过 90 分钟不再计入。要更准需要记录离开时刻（`visibilitychange`），代价是档案多一类事件。

## UI / UX 深度审视与精修（2026-09-09）

原始证据见 [深度评审](reviews/review-2026-09-09-ui-ux-deep-audit.md)；实现索引见路线图“学习体验精修”。

- [x] F01–F04：多选确认、持久化测验草稿、简答待评价和自动评分分母、漏答提交确认与覆盖率。
- [x] F05 的内容错绑：重排时原子重绑定文档与测验，改变已完成课程时拒绝更新。
- [ ] F05 后续：课程稳定 UUID 与跨目标稳定 URL；当前仍是索引链接，不应把内容重绑定当作完成身份迁移。
- [x] F06：阅读与测验分别就绪，出题失败不会阻塞已保存的教材。
- [x] F07–F08：导入深校验、目标和课程关系校验、替换预览与确认、导入前备份。
- [x] F09：创建目标表单与已成功阶段持久化，相同输入再次提交从 checkpoint 继续。
- [x] F10–F11：导师组合输入保护、清空确认；浏览器事件回归通过，实体输入法需设备验收。
- [x] F12：含行内代码、强调和链接的标题使用纯文本锚点，提供移动目录。
- [ ] F12 后续：目录统一基于 Markdown AST，覆盖复杂围栏和其他特殊语法，而不只依赖行解析。
- [x] F13–F14：长下一课标题与动作分离，Dialog 限高并内部滚动。
- [x] F15：新配置显式填写默认模型，清空字段时展示实际回退值；切换供应商清理地址和重设模型。
- [x] F16 的误导文案：仅存在配置时显示“模型已配置”，不宣称连接已验证；修改参数清除旧测试结果。
- [ ] F16 后续：两档模型分别测试与持久化验证状态，分类展示连接错误。
- [x] F17：先保存本地完成事实，再独立更新画像，支持评估失败重试且不重复记时（2026-09-19）。
- [x] F18：阅读区减少动态效果模式取消位移。
- [x] D01、D05、D06 的核心实现：回访学习书桌、当前课程优先、阅读头部精简、移动目录、章节恢复、单篇下载。
- [x] D09–D12 的局部实现：导师提问即时显示与滚动跟随控制、文库正文搜索、模型估计标识、触控目标与手机操作收拢。
- [x] D02–D12 中的目标切换、目标确认、技能关联测验与课程入口、并排导师和选段提问（2026-09-19）。
- [ ] D02–D12 后续：时间投入引导、更细的题目到技能证据归因、可操作技能关系图、跨目标文库与更多资料筛选。
- [x] 新增 12 条边界回归，覆盖测验、导入、重排、输入法、长文本、短视口、目录和草稿恢复。
- [ ] 测试债：修正原 seed 中课程与教材、画像与历史的语义不一致；补实体手机软键盘与 Safari 验证。

- [x] 首次创建目标页仅在本机已有目标且档案读取完成时显示 Goals 入口，避免空列表分流和加载闪现。
- [x] Sessions 默认 Adaptive：以目标、技能差距和偏好决定 1–10 课，同时保留手动课数及其草稿恢复。
- [x] 技能分析增加显式确认步骤：当前/目标水平滑块、3 分钟自动继续与延时、目标编辑后重新分析、刷新恢复和旧请求隔离。

- [x] 导师两种模式：随点随用与固定侧栏，保留模式偏好、共享草稿及生成状态，窄屏自动回退抽屉。

## 导航与对话精修（2026-09-19）

- [x] 快捷搜索在桌面/手机壳中重复挂载，快捷键打开两层弹窗；统一实例和焦点返回。
- [x] 当前目标仅为截断链接；增加可搜索目标选择器、真实课程进度和直接新建/管理入口。
- [x] 固定导师后目标、文库和画像仍按视口分栏；改为基于内容容器宽度。
- [x] 导师长草稿挤压历史、长代码撑宽气泡；限制输入高度、约束消息宽度，增加回到最新消息。
- [x] 搜索输入缺少明显焦点与触控空间；统一 44px 输入和选项，短视口内部滚动。

## 产品审计（2026-09-19）

原始证据见 [产品审计](reviews/review-2026-09-19-product-audit.md)；两项低成本修复（A06 题型解锁、A12 根级错误边界）的实现索引见路线图"产品审计与低成本修复"。优先级判断:P1 为 A01–A05,A01（数据耐久性）被历轮低估。

- [ ] A01：PWA 专项——manifest + Service Worker + 主屏安装引导（顺带离线阅读）+ `navigator.storage.persist()` + "距上次导出 N 天"提醒；WebKit 7 天脚本存储清除政策动手前先在实体 Safari 验证。
- [ ] A02：回访约定——本地通知、学习日程 .ics 导出、完成一课时约定下次复习时间并显示在首页；与 A01 合并为同一专项。
- [ ] A03：公开实例冷启动——replay fixture 包装为"免 key 试学一个示例目标"；配置页与生成前给成本量级提示（smart 档一课 4 次以上调用）。
- [ ] A04：教材 grounding 标示——阅读页注明"由模型生成,未联网核实"；BYOK 允许自带搜索 key；导师检索开关（`use_search` 现恒 false）。
- [ ] A05：prompt 加输出语言指令，随 M6 评测流程成批做并重录 fixture。
- [ ] A07：学习证据 → 路径重排建议的专项 spec（复习队列与画像变化 → 带解释、需确认的重排）；feedback simulator 决定产品化或删除。
- [ ] A08：完成当前课后 idle 预生成下一课，可取消，BYOK 成本 opt-in；checkpoint 机制已支持。
- [ ] A09：目标完成态的下一步——相邻目标建议、保持性复习计划；时间维度复习排序先定义语义。
- [ ] A10：技能跨目标归一聚合（`lib/learning-evidence.ts` 的 NFKC skillKey 可复用）→ 跨目标文库与技能总览；与上文 D02–D12 后续同源，以"终身学习者"定位论优先级上调，先立 spec。
- [ ] A11：profiler init 编造 `behavioral_patterns` 仍发模型，即上文 profiler 条目，随 A05 批量 prompt 迭代处理，不重复计。
- [ ] A13：长调用加客户端超时与取消（现仅导师可停、models 列表 15 秒）；复习确认屏 3 分钟自动继续的无人值守计费评估边界。
- [ ] A14：BYOK key 明文 localStorage，本地优先架构下不改代码，文档明示风险。
- [ ] A15：进展页证据环与模型折线同屏，补一句图注说明口径。
- [ ] A16：孤儿代码——`MasteryRing` 导出未使用，接入或删除；`useCompleteSession` 的 `completing` 恒 false，移除或接真实状态。
