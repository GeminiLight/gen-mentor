{
    "document": "# Working with Pandas DataFrames\n\nPandas is the foundational data manipulation library in Python, and the DataFrame is its central object. This session takes you from creating and inspecting DataFrames to grouping and summarizing a real sales dataset, closing with how to frame the findings for stakeholders.\n\n## Foundational Concepts\n\n### NumPy Arrays\n\nNumPy provides the ndarray, a fixed-type n-dimensional array that powers nearly every numeric operation in the Python data stack. Pandas is built on top of it, so understanding arrays makes DataFrame behavior — especially vectorized arithmetic and broadcasting — much easier to predict.\n\nCreate arrays with `np.array([1.5, 2.0, 2.5])` or helpers like `np.zeros` and `np.arange`. Operations apply element-wise without Python loops: `prices * 1.1` lifts every price by 10% in a single compiled pass. Aggregations such as `mean`, `sum`, and `std` return scalars, while comparisons like `prices > 100` produce the boolean masks used for filtering.\n\n### Pandas DataFrames\n\nA DataFrame is a two-dimensional, size-mutable table with labeled rows and columns, and it is the workhorse structure of the Pandas library. Each column is a Series that shares a common index, which is why selecting, filtering, and joining data feels consistent across the whole API.\n\nThe quickest way to build one from scratch is to pass a dictionary of column names to lists: `df = pd.DataFrame({\"product\": [...], \"sales\": [...]})`. More often you will load data directly with `pd.read_csv(\"sales.csv\")`, which exposes options for parsing dates, choosing an index column, and handling missing values at load time.\n\nInternalize the inspection methods early: `df.head()` previews the first rows, `df.info()` summarizes column types and non-null counts, `df.describe()` returns quick statistics for numeric columns, and `df.shape` reports the `(rows, columns)` tuple. Together they answer most questions you should ask before touching a new dataset.\n\n## Practical Applications\n\n### Data Aggregation with groupby\n\nThe split-apply-combine pattern is the heart of summarizing data. `df.groupby(\"region\")` splits rows into groups, an aggregation such as `.agg({\"sales\": \"mean\"})` computes a statistic per group, and Pandas combines the results into a new DataFrame indexed by the grouping key.\n\nChaining is idiomatic: `df.groupby(\"region\")[\"sales\"].sum().sort_values(ascending=False)` ranks regions by revenue in one readable line. Group by several columns at once by passing a list, for example `df.groupby([\"region\", \"quarter\"])`, to get a hierarchical index. The same mechanics power `pivot_table` when you want the result reshaped into a wide table for reporting.\n\n## Strategic Insights\n\n### Communicating Insights to Stakeholders\n\nAnalysis only creates value when stakeholders act on it, so plan the communication before polishing the chart. Lead with the decision the data supports, keep one message per visual, and annotate only the few data points that carry the argument instead of every value.\n\nFor executive audiences, prefer a ranked bar chart with clear units and a headline takeaway in the title — \"West region leads Q3 sales by 18%\" — over dashboards that require interpretation. Always state the time window, the filters applied, and any data-quality caveats; a single unexplained discrepancy can undo the credibility of an otherwise solid analysis.\n\n## Summary\n\nYou can now create DataFrames from dictionaries and CSV files, inspect them with head, info, describe, and shape, compute per-group statistics with groupby, and structure findings so decision-makers can act on them. The next session applies these tools to messy, real-world data cleaning.",
    "quizzes": {
        "single_choice_questions": [
            {
                "question": "Which Pandas method returns the first five rows of a DataFrame by default?",
                "options": [
                    "df.head()",
                    "df.tail()",
                    "df.first()",
                    "df.top()"
                ],
                "correct_option": 0,
                "explanation": "head(n=5) previews the top of a DataFrame; tail() returns the last rows instead."
            },
            {
                "question": "What does the shape attribute of a DataFrame return?",
                "options": [
                    "The column labels as a list",
                    "A tuple of (number of rows, number of columns)",
                    "The total number of cells",
                    "The data type of each column"
                ],
                "correct_option": 1,
                "explanation": "df.shape is a (rows, columns) tuple; df.dtypes is what lists the column types."
            },
            {
                "question": "Which expression computes the average of the sales column for each region?",
                "options": [
                    "df.groupby(\"region\")[\"sales\"].mean()",
                    "df[\"sales\"].mean(\"region\")",
                    "df.mean(group=\"region\")",
                    "df.sort_values(\"sales\").mean()"
                ],
                "correct_option": 0,
                "explanation": "groupby splits rows by region, the column selection picks sales, and mean() aggregates each group."
            }
        ],
        "multiple_choice_questions": [
            {
                "question": "Which statements about df.groupby are correct? (Select all that apply)",
                "options": [
                    "It implements the split-apply-combine pattern",
                    "It can group by several columns when given a list",
                    "It requires the DataFrame to be sorted first",
                    "Aggregations like sum() or mean() can be chained after it"
                ],
                "correct_options": [0, 1, 3],
                "explanation": "Sorting is not required — grouping handles unordered rows; the other three statements describe standard groupby usage."
            }
        ],
        "true_false_questions": [
            {
                "question": "In Pandas, a Series is a one-dimensional labeled array, while a DataFrame is two-dimensional.",
                "correct_answer": true,
                "explanation": "A DataFrame is a collection of Series that share one index, one Series per column."
            }
        ],
        "short_answer_questions": [
            {
                "question": "Which DataFrame method computes count, mean, std, and quartiles for all numeric columns at once?",
                "expected_answer": "describe",
                "explanation": "df.describe() returns summary statistics for every numeric column by default."
            }
        ]
    }
}
